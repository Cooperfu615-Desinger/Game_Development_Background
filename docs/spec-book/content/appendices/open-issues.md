# 待決策與校正清單

## 跨模組待決策

| 項目 | 責任方 | 阻擋範圍 |
|---|---|---|
| Provider 內部角色與 permission key | 產品／後端 | 正式權限與上線 |
| Game Round 正式狀態枚舉 | 產品／後端／GGAP | 後端與整合 |
| 點數與 USDT 精度、四捨五入 | Provider／後端／GGAP | 財務與正式上線 |
| GGAP 正式 API、簽章、Callback、ACK 與重試 | GGAP／後端 | 對接與正式上線 |
| 資料保存、查詢與匯出上限 | 產品／後端 | 後端與營運 |

## 現行文件與程式校正

- 遊戲版本與數值 mock 仍有「影響商戶」「全部正式商戶」等舊語意。
- 設定總覽與操作紀錄仍有平台結算、發票、商戶及錢包語意。
- DEMO 大廳使用 USD 與 Session，需明確定義為隔離展示模型。
- 遊戲紀錄部分主要標籤仍使用英文，需依繁體中文顯示規則統一。
- 儀表板曾使用 `event_id`，風控報表正式 query 使用 `risk_event_id`。
- 遊戲管理的文件順序與實際左側選單順序需統一。

以上差異先記錄，不在規格網站結構階段直接修改 Provider Portal 原型。
